import { ButtonVariants } from "./buttonVariants";
import { ButtonSizes } from "./buttonVariants";
export const buttonConfig = {
    defaultVariant: ButtonVariants.PRIMARY,
    defaultSize: ButtonSizes.MD,
    rounded: true,
    fullWidth: false
};